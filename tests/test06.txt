program teste6
    declare
        first, second : int;
    begin
        first=1;
        second=1;
        out(first)
        out(second)
        while(first < 10)
            first = second + first
            second = second + first
            out(first)
            out(second)
        end
    end